# sPDF 1.30.1 릴리스 노트

릴리스 날짜: 2026-09-05

## 1.28.0 주요 변경 - 2026-09-02

### 성능 개선

- Soft Light·Multiply·Screen·Overlay 등 11개 혼합 모드를 사용하는 격리된 PDF 그룹도 기존 배경과 그룹 불투명도를 유지하면서 GPU 경로로 표시합니다.

## 1.28.1 개선 - 2026-09-02

- 격리된 PDF 그룹의 색조(Hue)·채도(Saturation)·색상(Color)·명도(Luminosity) 혼합도 기존 배경과 그룹 불투명도를 유지하면서 GPU 효과로 처리합니다.

## 1.28.2 개선 - 2026-09-02

- 중첩된 도형·텍스트 클리핑 안의 PDF 혼합 그룹도 기존 배경과 잘린 경계를 유지하면서 GPU 경로로 표시합니다.

## 1.28.3 개선 - 2026-09-02

- 알파·밝기·이미지 마스크 안의 혼합과 클리핑도 GPU로 처리하며, 중첩 마스크를 지원합니다.
- 색상 밝기 마스크는 PDF 마스크 전용 색 변환을 적용하고, 회색조 마스크 이미지는 지정된 이미지 보간 설정을 유지합니다.
- 비격리·knockout 그룹과 마스크 보정 함수는 CPU 대체 경로를 유지합니다.

## 1.28.4 개선 - 2026-09-02

- 독립 실행 창의 문서 탭을 제목표시줄로 옮기고 `파일.pdf [읽기/GPU]`, `[편집/CPU]`처럼 표시합니다.
- CPU/GPU 표시를 합성이 아닌 실제 래스터화 기준으로 바꿨습니다. CPU 이미지는 CPU, 혼합 래스터화는 CPU+GPU로 표시합니다.

## 1.28.5 개선 - 2026-09-03

- GPU로 표시하는 페이지의 벡터와 글자 윤곽 가장자리가 부드럽게 보이도록 그레이스케일 안티앨리어싱을 명시적으로 유지합니다.
- GPU 장면 준비가 오래 걸리는 어려운 페이지는 짧게 기다린 뒤 CPU 표시 경로로 전환해 리더가 오래 멈추지 않게 했습니다.
- 일반적인 선형·방사형 PDF 그라데이션은 더 적은 그리기 작업으로 GPU 경로에 남습니다.

## 1.28.6 개선 - 2026-09-03

- 일부 논문 PDF처럼 표시 목록의 glyph id가 빠진 글자도 내장/원본 폰트가 문자를 벡터 glyph로 다시 매핑할 수 있으면 GPU 경로에 남습니다.
- GPU로 표시된 페이지 위의 임시 Direct2D ABI 화면 배지는 더 이상 표시하지 않습니다.

## 1.29.0 새 기능 - 2026-09-03

- 편집 모드에서 현재 쪽의 캔버스 크기를 조정할 수 있습니다.
- 기존 개체 크기를 유지한 채 현재 쪽의 도련 박스를 설정할 수 있습니다.
- 편집 가능한 텍스트 요소를 편집기 우클릭 메뉴에서 크기 조절할 수 있습니다.
- Direct2D 렌더러가 독립된 미지원 투명도 그룹을 CPU island 이미지로만 래스터화합니다. 어려운 국소 knockout 효과에는 작은 근사 island를 허용해 GPU 강제 모드에서 GPU 표시를 유지하면서도 페이지 수치와 좌표는 PDF 모델 기준을 유지합니다.
- GPU 강제 모드에서는 자동 모드보다 긴 장면 준비 예산을 사용해, 복잡한 페이지가 CPU 경로로 물러서기 전에 GPU 장면 추출을 끝낼 시간을 더 줍니다.

## 1.29.1 개선 - 2026-09-04

- PDF soft-mask transfer function을 GPU 알파 테이블로 적용해 Direct2D 경로에 남깁니다. 네이티브 렌더러 ABI는 v17입니다.
- 단순 색상 벡터 타일 패턴을 Direct2D 장면 항목으로 펼쳐 페이지 전체 CPU 대체 없이 표시합니다.
- 근사 CPU island가 작은 겹침 도형을 같은 제한 영역 래스터 island에 흡수해 GPU 강제 렌더링을 유지하면서 중복 벡터 경계선을 줄입니다.
- 같은 색으로 연속 배치된 글자 glyph outline을 페이지 좌표의 결합 path로 압축해 PDF 모델 좌표는 유지하면서 GPU 장면 항목과 네이티브 path 리소스를 줄입니다.
- linear/radial gradient primitive를 감싼 중복 clip wrapper가 gradient 영역과 같으면 제거해, gradient가 많은 페이지의 장면 명령을 줄입니다.
- 기본값이 꺼진 실험용 유사 색상 band 병합으로, 기본 정확 색상 경로와 분리된 장면 캐시에서 GPU 렌더러 압축 효과를 비교할 수 있습니다.
- 확대·축소 중에는 현재 GPU 장면을 정확한 행렬로 즉시 변환하고 입력이 안정된 뒤 이미지 품질을 갱신해, 매 확대 단계의 장면 추출 대기를 없애면서 배율과 페이지 좌표는 정확하게 유지합니다.

## 1.29.2 개선 - 2026-09-04

- Ctrl/Alt+휠 확대·축소가 현재 GPU 장면을 정확한 중간 페이지 변환으로 부드럽게 표시한 뒤 요청한 배율에서 끝납니다. 이미지 품질 장면은 입력이 안정된 뒤 갱신합니다.

## 1.29.3 성능 개선 - 2026-09-04

- Direct2D 페이지 장면을 ABI v18 네이티브 영역에 보관해 프레임마다 Python에서 네이티브로 한 번만 호출합니다. bitmap 기반 마스크·혼합 캡처가 없는 장면은 재사용 가능한 Direct2D 명령 목록으로도 컴파일합니다.

## 1.29.4 성능 개선 - 2026-09-05

- 이미지 마스크와 도형 클립이 전체 창을 반복 복사하지 않고 제한된 임시 GPU 합성 표면을 사용해, 정확한 페이지 변환을 유지하면서 복잡한 포스터·리플렛의 화면 반응을 개선합니다.

## 1.29.5 성능 개선 - 2026-09-05

- 마스크나 혼합 그룹이 있는 페이지에서도 일반 도형 클립은 불필요한 임시 GPU 합성 표면을 만들지 않습니다.
- retained GPU 장면이 그라데이션 자원을 프레임 사이에 재사용해, 그라데이션이 많은 페이지의 확대·이동 반응을 크게 개선합니다.
- 자동·GPU 강제 모드에서 단순 사각형 clip을 가속하면서 페이지 좌표와 변환은 정확하게 유지합니다. 네이티브 렌더러 ABI는 v19입니다.
- 자동 모드는 매우 복잡한 쪽을 빠르게 판별해 CPU 화면을 먼저 표시하고, 백그라운드 준비가 끝나면 GPU 장면으로 자연스럽게 교체합니다.
- 문서 탭이 화면에 나타날 때 미뤄진 GPU 준비를 확실히 시작하며, 렌더링 진단에서 현재 CPU/GPU 경로와 백그라운드 GPU 준비 상태를 명확하게 구분합니다.

## 1.29.6 버그 수정 - 2026-09-05

- CPU·GPU 모드에서 확대 후 배율 표시가 되돌아가는 문제를 수정했습니다.
- 배율 표시와 입력에 화면 DPI를 반영하고, 다시 여는 문서도 처음에는 창 너비에 맞춥니다.

- Windows 150% 등 화면 배율이 독립 실행 UI에 반영되도록 수정했습니다.
- CPU 렌더링과 OpenGL 합성을 구분하고 Direct2D 초기화 실패 이유를 진단 툴팁에 표시합니다.

## 1.29.7 개선 - 2026-09-05

- 렌더링 진단에 CPU 담당 작업을 함께 표시합니다. 표시 경로에 따라 타일 렌더링, GPU 장면 준비, 이미지 처리 등을 확인할 수 있습니다.

- GPU가 직접 그리는 그라데이션을 CPU 비트맵 작업으로 잘못 표시하던 문제를 수정했습니다. 실제 비트맵 그라데이션과 CPU 합성 영역을 구분합니다.

## 1.30.0 새 기능 - 2026-09-05

- 공용 100MiB 디스크 캐시로 같은 렌더링 배율에서 GPU 장면을 재사용합니다. 오래된 항목은 자동 정리하고 문서 변경·암호화·편집 내용에 이전 장면을 적용하지 않습니다.
- 저장 위치: %LOCALAPPDATA%\sPDF\cache\gpu-scenes.sqlite3. 앱을 종료한 뒤 삭제하면 다음 실행에서 다시 생성합니다.

## 1.30.1 개선 - 2026-09-06

- 일반 이미지는 디코딩한 픽셀 대신 원본 PDF 참조로 캐시하고, 그리기 명령과 재계산 비용이 큰 합성 비트맵은 유지합니다.
- 렌더링 설정에서 디스크 캐시를 끄거나 50·100·250·500MB로 선택합니다. 용량을 줄이면 오래된 항목부터 즉시 정리합니다.

- 색상·알파 값을 그대로 유지하면서 이미지 준비를 빠르게 처리합니다. 반복 이미지는 GPU 업로드를 공유하고 메모리 캐시 예산에 벡터·글자 명령도 포함합니다.
- 자동 모드에서 확대용 고해상도 장면은 별도 프로세스로 준비하며 현재 화면을 유지합니다.
